package dev.skrilltrax.blockka.data.manager

import android.content.Context

class IncomingCallManager29Impl(context: Context): IncomingCallManager {

    override fun rejectCall(number: String): Boolean {
        return false
    }
}
